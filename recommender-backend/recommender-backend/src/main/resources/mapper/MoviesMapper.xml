<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nbu.lwf.recommenderbackend.dao.MoviesDao">
    
    <select id="getmovies" resultType="nbu.lwf.recommenderbackend.entity.Movies">
        select * from movies
        <where>

        </where>
    </select>

</mapper>